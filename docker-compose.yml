version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - APP_DEBUG=true
    env_file:
      - .env
    volumes:
      - .:/app
    command: python main.py

